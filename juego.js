const prompt = require('prompt-sync')();

var palabraOculta= [];
var palabraMostrada= ["*","*","*","*"];

var contadorFallas = 4;
var contadorAciertos= 0;

palabraOculta[0]=prompt("dame la primera letra: ");
palabraOculta[1]=prompt("dame la segunda letra: ");
palabraOculta[2]=prompt("dame la tercera letra: ");
palabraOculta[3]=prompt("dame la cuarta letra: ");

//console.log(palabraOculta);

if()
